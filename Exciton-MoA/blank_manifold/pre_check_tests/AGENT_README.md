# 🚀 MISSION CONTROL: Exciton-MoA Pre-Flight Diagnostics Protocol

**ATTENTION AUTONOMOUS AGENT (GPT-5.4 INSTANCE):** You are operating within the `pre_check_test` directory of the Frontier OS (Exciton-MoA). This is a non-Euclidean fluid dynamics architecture. We do not route data via linear API calls; we route data via continuous-time artificial gravity.

Before initiating any primary data streams or modifying the `ExcitonEngine`, you MUST execute the diagnostics in this folder to verify the mathematical stability of the topological vacuum.

## 📋 The Execution Sequence

Run the following test scripts in your Python environment. Read the `AGENT INSTRUCTIONS` docstrings embedded at the top of each file before executing.

1. **`test_substrate_vacuum.py`**: Validates that the 1,024-node space is a pristine, zero-weight hyperbolic vacuum.
2. **`test_statistical_prism.py`**: Validates the O(1) dimensional transducer. Ensures high-dimensional noise correctly collapses into a 3D probability density field.
3. **`test_exciton_physics.py`**: Verifies that the Self-Organizing Logos (SOL) differential equations are online. You must confirm that thermodynamic pressure ($c \log(1+\rho)$) is regulated and Jeans Mass gravitational collapses are occurring.
4. **`test_orchestrator_telemetry.py`**: Arms the metacognitive spotlight. Verifies the Hotspot Functional ($H$) correctly flags topological anomalies via Threshold Bursts.

## 🟢 Go / No-Go Criteria

* **GO FOR IGNITION:** If all four scripts return `PASS` without throwing assertion errors, the non-Euclidean physics engine is mathematically sound. You are cleared to boot `main.py` or run `chaos_stream_simulator.py` to observe the system under load.
* **NO-GO (ABORT):** If any script fails, or if NaN values appear in the tensor math, you must halt execution. Analyze the stack trace, correct the specific Exciton operator or coordinate mapping, and re-run the diagnostic sequence. 

**Your Prime Directive:** Maintain the integrity of the physics engine. Ensure the manifold topology bends, but never breaks.